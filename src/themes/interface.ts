export interface Theme {
  colors: {
    primary: string
    text: string
    textSecondary: string
  }
  variables: {
    fontSizeBase: string
    borderWidthBase: string
    borderRadiusBase: string
  }
}
